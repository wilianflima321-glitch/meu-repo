# =============================================================================
# Aethel Engine - Configuração de Ambiente
# =============================================================================
# ATENÇÃO: Copie este arquivo para .env e configure os valores!
# NUNCA commite o arquivo .env no repositório!
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_USER=aethel
POSTGRES_PASSWORD=__CHANGE_ME_STRONG_PASSWORD__
POSTGRES_DB=aethel_db

# Connection URL (gerada automaticamente, mas pode customizar)
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# -----------------------------------------------------------------------------
# CACHE (Redis)
# -----------------------------------------------------------------------------
# REDIS_URL=redis://redis:6379
# REDIS_PASSWORD=__OPTIONAL_REDIS_PASSWORD__

# -----------------------------------------------------------------------------
# AUTHENTICATION & SECURITY
# -----------------------------------------------------------------------------
# Gere um JWT secret seguro: openssl rand -base64 64
JWT_SECRET=__GENERATE_SECURE_SECRET_64_BYTES__

# Session secret (se diferente do JWT)
# SESSION_SECRET=__ANOTHER_SECURE_SECRET__

# -----------------------------------------------------------------------------
# APPLICATION
# -----------------------------------------------------------------------------
NODE_ENV=production
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Server local do Aethel (health/dependencies)
NEXT_PUBLIC_AETHEL_SERVER_URL=http://localhost:1234
AETHEL_SERVER_URL=http://localhost:1234

# API Rate limiting
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=100

# -----------------------------------------------------------------------------
# EXTERNAL SERVICES (Opcional)
# -----------------------------------------------------------------------------
# OpenAI API Key (para AI Coding Assistant)
# OPENAI_API_KEY=sk-...

# Anthropic API Key (para Claude)
# ANTHROPIC_API_KEY=sk-ant-...

# GitHub OAuth (para login com GitHub)
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# STORAGE (S3-compatible)
# -----------------------------------------------------------------------------
# S3_ENDPOINT=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_BUCKET=aethel-assets
# S3_REGION=us-east-1

# -----------------------------------------------------------------------------
# MONITORING & LOGGING
# -----------------------------------------------------------------------------
# SENTRY_DSN=
# LOG_LEVEL=info

# -----------------------------------------------------------------------------
# ASSET PIPELINE (Opcional)
# -----------------------------------------------------------------------------
# Caminho para gltf-transform (CLI) para simplificação de malhas
# GLTF_TRANSFORM_PATH=
# Caminho para gltf-pipeline (CLI) para compressão Draco
# GLTF_PIPELINE_PATH=
# Limite máximo de download de assets (bytes)
# AETHEL_MAX_ASSET_DOWNLOAD_BYTES=2147483648
# Limite máximo de cache local (bytes)
# AETHEL_MAX_CACHE_BYTES=10737418240

# -----------------------------------------------------------------------------
# EXPORT PIPELINE (Opcional)
# -----------------------------------------------------------------------------
# Incluir assets locais (public/uploads/<projectId>) no ZIP de export
# EXPORT_INCLUDE_ASSETS=true
# Limite total de bytes de assets no ZIP
# EXPORT_MAX_ASSET_BYTES=1073741824
# Limite por arquivo de asset no ZIP
# EXPORT_MAX_SINGLE_ASSET_BYTES=209715200
# Limite de assets com presigned URL no manifesto
# EXPORT_MAX_PRESIGN_ASSETS=250
# Expiração das presigned URLs (segundos)
# EXPORT_PRESIGN_EXPIRES_IN=3600
# Gerar LODs para GLB/GLTF durante export
# EXPORT_GENERATE_LODS=true
# Ratios de simplificação (CSV)
# EXPORT_LOD_LEVELS=0.5,0.25,0.1
# Tamanho máximo do arquivo fonte para gerar LOD (bytes)
# EXPORT_LOD_MAX_SOURCE_BYTES=209715200
# Limite máximo de arquivos LOD gerados
# EXPORT_LOD_MAX_FILES=500
# Limite total de bytes de source files no ZIP
# EXPORT_MAX_SOURCE_BYTES=209715200
# Limite total de source files no ZIP
# EXPORT_MAX_SOURCE_FILES=5000
# Caminho para templates de runtime (windows/linux/macos)
# RUNTIME_TEMPLATES_DIR=

# -----------------------------------------------------------------------------
# AUTO-UPDATER (Delta Patch Opcional)
# -----------------------------------------------------------------------------
# Caminho para bspatch (bsdiff)
# BSPATCH_PATH=
# Caminho para xdelta3 (fallback)
# XDELTA_PATH=

# -----------------------------------------------------------------------------
# DIAGNOSTICS (Opcional)
# -----------------------------------------------------------------------------
# Token para baixar logs via /api/diagnostics/logs
# AETHEL_DIAGNOSTIC_TOKEN=

# -----------------------------------------------------------------------------
# KUBERNETES / PRODUCTION
# -----------------------------------------------------------------------------
# K8S_NAMESPACE=aethel-prod
# REPLICA_COUNT=3
