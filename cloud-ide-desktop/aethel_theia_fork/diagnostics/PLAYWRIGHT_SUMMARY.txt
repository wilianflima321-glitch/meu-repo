Playwright local run summary (chromium):

14 passed, 2 skipped

Tests executed (selected):
- run-ensemble should fail when scene violates ensemble constraints
- run-ensemble should succeed when scene satisfies constraints
- verify scene rejects weapon when no_weapons constraint present
- verify scene accepts when no forbidden items present
- ensemble runtime creates usage events for member providers
- create ensemble via mock API and verify
- run-ensemble rejects when scene violates constraints and succeeds otherwise
- run-ensemble soft mode returns warnings but creates usage events
- run-ensemble returns warnings and records usage events (soft-warn-e2e)
- health endpoint returns ok
- items list returns array
- echo returns posted JSON
- root path returns plain text status

Notes:
- Mock server started locally and responded on http://127.0.0.1:8010
- No verifier-debug.json was generated in this local run (no failing verifier trace persisted). If you want me to force a trace, I can run targeted requests to generate it.
