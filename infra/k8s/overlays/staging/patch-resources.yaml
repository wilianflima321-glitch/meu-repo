apiVersion: apps/v1
kind: Deployment
metadata:
  name: aethel-web
spec:
  template:
    spec:
      containers:
        - name: aethel-web
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
