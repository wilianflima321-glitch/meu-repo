// Disabled temporary root flat ESLint config. Renamed by automated patch
// to allow per-package legacy `.eslintrc.*` files to be used by ESLint CLI
// during an incremental migration.
//
// If you need to re-enable flat-config, restore this file to
// `eslint.config.cjs` and remove the `.disabled` suffix.

module.exports = {};
