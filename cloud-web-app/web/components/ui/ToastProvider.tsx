'use client';

/**
 * Canonical toast entry point for simple product notifications.
 * Advanced queue and persistence behavior lives in `./toast-system`.
 */
export { ToastProvider, useToast } from './Toast'
export { default } from './Toast'
