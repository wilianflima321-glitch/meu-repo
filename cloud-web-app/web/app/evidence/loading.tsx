import { RouteLoading } from "@/components/route-boundaries/RouteLoading";

export default function Loading() {
  return <RouteLoading title="Loading Evidence" detail="Preparing receipts, release lanes, blockers, and review packets." />;
}
