import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Creative Studio Hub | Aethel Engine',
  description: 'Visual project orchestration, AI agents, asset pipeline, and director tools — all without touching code.',
}

export { default } from './page'
