"use client";

import { RouteError } from "@/components/route-boundaries/RouteError";

export default function Error({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) {
  return <RouteError error={error} reset={reset} title="Admin Console recovered safely" detail="Admin routes are isolated so operational failures do not break the product shell." />;
}
