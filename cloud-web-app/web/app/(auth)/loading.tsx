import { RouteLoading } from "@/components/route-boundaries/RouteLoading";

export default function Loading() {
  return <RouteLoading title="Loading secure entry" detail="Preparing the compact auth door and workspace return target." />;
}
