import { RouteLoading } from "@/components/route-boundaries/RouteLoading";

export default function Loading() {
  return <RouteLoading title="Loading IDE" detail="Starting editor, agents, previews, and project context." />;
}
