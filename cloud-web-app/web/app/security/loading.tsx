import { RouteLoading } from "@/components/route-boundaries/RouteLoading";

export default function Loading() {
  return <RouteLoading title="Loading Security" detail="Preparing trust controls, posture notes, and public links." />;
}
