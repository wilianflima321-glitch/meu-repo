import { CaptureSystem } from './runtime';
import { CaptureProvider, useCanvasCapture, useCaptureGallery, useCaptureSystem, usePhotoMode, useScreenshot, useVideoRecording } from './react';
import { PHOTO_FILTER_PRESETS } from './types';

const __defaultExport = {
  CaptureSystem,
  CaptureProvider,
  useCaptureSystem,
  useScreenshot,
  useVideoRecording,
  usePhotoMode,
  useCaptureGallery,
  useCanvasCapture,
  PHOTO_FILTER_PRESETS,
};

export default __defaultExport;
